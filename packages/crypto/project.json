{
  "name": "crypto",
  "sourceRoot": "packages/crypto/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/crypto && npx tsc"
      },
      "dependsOn": ["shared:build"]
    }
  }
}
