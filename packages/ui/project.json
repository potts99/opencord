{
  "name": "ui",
  "sourceRoot": "packages/ui/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/ui && npx tsc"
      },
      "dependsOn": ["shared:build"]
    }
  }
}
