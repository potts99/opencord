{
  "name": "shared",
  "sourceRoot": "packages/shared/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/shared && npx tsc"
      }
    }
  }
}
