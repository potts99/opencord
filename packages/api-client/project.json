{
  "name": "api-client",
  "sourceRoot": "packages/api-client/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/api-client && npx tsc -b"
      },
      "dependsOn": ["shared:build"]
    }
  }
}
